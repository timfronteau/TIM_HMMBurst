lags = np.arange(-29, 29) i.e. [-29, -28, ..., 27, 28]  # Time-delay embedding
n_components = 40     # For the principal component analysis
n_states = 3 to 6    # for the Hidden Markov Model

covariance_type = 'diag'    # Type of the inferred covariance matrix : delays*IC params are inferred
model_type = 'GaussianHMM'  # Observation model (here, a means-array and a covariance-matrix is inferred)
n_iter = 100
tol = 0.01
downsamp_rate = 1 (there is no downsampling)

BEFORE (concatenating the (-4s to 0s)-timecourses of the epoched trials):

|_ (58delays)  x (4s x 256Hz x Ntrials)
            -----PCA-----> |_ (40components) x (7s x 256Hz x Ntrials) 
            -------------> TDE-HMM ( 1 Gaussian obs. model per state, 3 to 6 states )
            
AFTER (concatenating the (0s to 3s)-timecourses of the epoched trials):

|_ (58delays)  x (3s x 256Hz x Ntrials)
            -----PCA-----> |_ (40components) x (7s x 256Hz x Ntrials) 
            -------------> TDE-HMM ( 1 Gaussian obs. model per state, 3 to 6 states )
            
